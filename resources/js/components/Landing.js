import React, { Fragment } from 'react';
import ReactDOM from 'react-dom';

const Landing = (prop) => {
    return (
        <div>
            Landing
        </div>
            );
}

export default Landing;

